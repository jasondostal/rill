# Rill Sidecar config. Copy to .env (gitignored). build.sh seeds these into the
# app's prefs domain on first build; you can also set them in Settings.
RILL_HOST=https://rill.example.com
RILL_TOKEN=rill_your_token_here
